name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verify imports
        run: |
          python -c "from pqcrypto.kem.ml_kem_768 import generate_keypair; print('ML-KEM-768 OK')"
          python -c "from pqcrypto.sign.ml_dsa_65 import generate_keypair; print('ML-DSA-65 OK')"
          python -c "from cryptography.hazmat.primitives.ciphers.aead import AESGCM; print('AES-GCM OK')"
          python -c "from argon2.low_level import hash_secret_raw; print('Argon2 OK')"

      - name: Run encrypt/decrypt round-trip test
        run: python tests/test_roundtrip.py
